{
  "name": "donation-server",
  "version": "1.0.0",
  "description": "A simple server to handle donations",
  "main": "./api/index.js",
  "scripts": {
    "start": "node --env-file=.env ./api/index.js",
    "dev": "node --watch --env-file=.env ./api/index.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.17.1"
  }
}
